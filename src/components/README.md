# Components

For reusable React components used throughout the application.

- `/charts` - Chart.js visualizations for stock data
- `/ui` - Basic UI elements like buttons, cards, inputs
- `/layout` - Page layout components like headers and footers

